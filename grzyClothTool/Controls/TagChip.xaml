<UserControl x:Class="grzyClothTool.Controls.TagChip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:grzyClothTool.Controls"
             x:Name="MyTagChip"
             mc:Ignorable="d"
             d:DesignHeight="20" d:DesignWidth="80">
    <Border Background="{DynamicResource Brush300}"
            BorderBrush="{DynamicResource Brush500}"
            BorderThickness="1"
            CornerRadius="10"
            Padding="8,2"
            Margin="2">
        <TextBlock Text="{Binding TagText, ElementName=MyTagChip, Converter={StaticResource UpperCaseConverter}}"
                   FontSize="10"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource Brush900}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>
    </Border>
</UserControl>
